"""EASE Plugins — concrete implementations of every plugin category.

All plugins register themselves via packaging entry-points (see pyproject.toml)
and are discovered at runtime by the PluginRegistry.
"""
