"""End-to-end tests — full pipeline run against a fixture repository.

These tests verify that the orchestrator, plugins, and storage work together
correctly from project registration through to comparison report generation.
"""
